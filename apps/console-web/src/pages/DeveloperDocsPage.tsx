import { useMemo, useState } from 'react'
import { ArrowRight, CheckCircle2, Copy, KeyRound, Network, ShieldCheck, TriangleAlert } from 'lucide-react'
import { Link } from 'react-router-dom'
import { Button, useToast } from '../components/ui'

type ExampleLanguage = 'curl' | 'python' | 'typescript'

export function DeveloperDocsPage() {
  const toast = useToast()
  const [language, setLanguage] = useState<ExampleLanguage>('curl')
  const baseURL = `${window.location.origin}/v1`
  const examples = useMemo<Record<ExampleLanguage, string>>(() => ({
    curl: `curl ${baseURL}/responses \\\n  -H "Authorization: Bearer $RELAYDOCK_API_KEY" \\\n  -H "Content-Type: application/json" \\\n  -H "Idempotency-Key: $(uuidgen)" \\\n  -d '{\n    "model": "<published-model-alias>",\n    "input": "Hello from ModelDock"\n  }'`,
    python: `import os\nfrom openai import OpenAI\n\nclient = OpenAI(\n    api_key=os.environ["RELAYDOCK_API_KEY"],\n    base_url="${baseURL}",\n)\n\nresponse = client.responses.create(\n    model="<published-model-alias>",\n    input="Hello from ModelDock",\n)\nprint(response.output_text)`,
    typescript: `import OpenAI from "openai";\n\nconst client = new OpenAI({\n  apiKey: process.env.RELAYDOCK_API_KEY,\n  baseURL: "${baseURL}",\n});\n\nconst response = await client.responses.create({\n  model: "<published-model-alias>",\n  input: "Hello from ModelDock",\n});\nconsole.log(response.output_text);`,
  }), [baseURL])
  return <div className="developer-docs"><div className="public-container developer-docs-layout"><aside className="developer-docs-nav" aria-label="文档目录"><strong>开始接入</strong><a href="#quickstart">快速开始</a><a href="#base-url">SDK base_url</a><a href="#authentication">认证</a><strong>请求</strong><a href="#ordinary-request">普通请求</a><a href="#sse">SSE 流式</a><a href="#embeddings">Embeddings</a><strong>行为与政策</strong><a href="#errors">错误码</a><a href="#rate-limits">限流</a><a href="#idempotency">幂等</a><a href="#billing">计费</a><a href="#byok">BYOK</a><a href="#retention">数据保留</a><a href="#fallback">Provider fallback</a></aside><article className="developer-docs-content"><header className="developer-docs-hero"><span><Network size={22} /></span><div><small>MODELDOCK API</small><h1>开发者文档</h1><p>将 OpenAI SDK 的 base URL 指向 ModelDock，使用项目级 <code>rdk_*</code> 密钥调用当前项目获准的模型别名。</p></div></header><section id="quickstart"><h2>快速开始</h2><p>先在 <Link to="/models">公开模型目录</Link> 核对地区和价格，再在 onboarding 中创建组织、项目、充值与 API Key。完整密钥只显示一次，应保存在服务端密钥管理器或环境变量中。</p><CodeTabs value={language} onChange={setLanguage} code={examples[language]} onCopy={() => { void navigator.clipboard.writeText(examples[language]); toast('示例已复制') }} /><div className="docs-callout warning"><TriangleAlert size={17} /><div><strong>替换示例中的占位值</strong><p><code>&lt;published-model-alias&gt;</code> 必须来自你的项目模型列表。不要把真实密钥写入源码、浏览器脚本、日志或聊天消息。</p></div></div></section><section id="base-url"><h2>OpenAI SDK base_url 替换</h2><p>保留 SDK 的请求模型，只替换 base URL 与 API Key。默认反向代理部署下，数据面地址为当前站点的 <code>/v1</code>；若运维将数据面部署到独立域名，请使用控制台给出的网关地址。</p><pre className="docs-code-static"><code>OpenAI(api_key=os.environ["RELAYDOCK_API_KEY"], base_url="{baseURL}")</code></pre><p>Python 参数名是 <code>base_url</code>，JavaScript/TypeScript 参数名是 <code>baseURL</code>。</p></section><section id="authentication"><h2>认证</h2><p>每次网关请求发送项目级 RelayDock/ModelDock 密钥：</p><pre className="docs-code-static"><code>Authorization: Bearer rdk_live_&lt;secret&gt;</code></pre><ul className="docs-checklist"><li><CheckCircle2 />生产与测试环境可分别使用 <code>rdk_live_*</code> 与 <code>rdk_test_*</code>。</li><li><CheckCircle2 />密钥可限制模型、RPM、TPM 和有效期。</li><li><CheckCircle2 />Provider 上游密钥不是下游认证凭据。</li></ul></section><section id="ordinary-request"><h2>普通请求</h2><p>推荐新集成使用 <code>POST /v1/responses</code>；已有兼容客户端可继续使用 <code>POST /v1/chat/completions</code>。</p><div className="docs-two-column"><div><h3>Responses</h3><pre className="docs-code-static"><code>{`{\n  "model": "<published-model-alias>",\n  "input": "Summarize this text"\n}`}</code></pre></div><div><h3>Chat Completions</h3><pre className="docs-code-static"><code>{`{\n  "model": "<published-model-alias>",\n  "messages": [\n    {"role": "user", "content": "Hello"}\n  ]\n}`}</code></pre></div></div><p>每个响应都包含 <code>X-Request-Id</code> 与 <code>X-RelayDock-Request-Id</code>。你可发送最长 512 字节的 <code>X-Client-Request-Id</code> 做调用关联，但它不会替代服务端生成的可信请求 ID。</p></section><section id="sse"><h2>SSE 流式请求</h2><p>在 Responses 或 Chat Completions 请求体中设置 <code>"stream": true</code>，并使用支持逐块读取的 HTTP 客户端。不要在每个事件到达前缓冲整个响应。</p><pre className="docs-code-static"><code>{`curl -N ${baseURL}/responses \\\n  -H "Authorization: Bearer $RELAYDOCK_API_KEY" \\\n  -H "Content-Type: application/json" \\\n  -d '{"model":"<published-model-alias>","input":"Hello","stream":true}'`}</code></pre><p>流式事件保持上游兼容格式。客户端应处理网络中断、非 2xx 初始响应以及事件流中的终止标记；只有服务端完成结算后，控制台才显示最终 Token 与扣费。</p></section><section id="embeddings"><h2>Embeddings</h2><p>使用项目已授权且公开能力包含 embeddings 的模型别名：</p><pre className="docs-code-static"><code>{`curl ${baseURL}/embeddings \\\n  -H "Authorization: Bearer $RELAYDOCK_API_KEY" \\\n  -H "Content-Type: application/json" \\\n  -d '{"model":"<embedding-model-alias>","input":["first document","second document"]}'`}</code></pre></section><section id="errors"><h2>错误码</h2><p>错误尽可能使用 OpenAI-style envelope：<code>{'{"error":{"message","type","param","code"}}'}</code>。排障时记录 HTTP 状态、error.code 与响应头中的 RelayDock 请求 ID，不要记录 API Key 或提示内容。</p><div className="public-table-wrap"><table className="public-table"><thead><tr><th>HTTP</th><th>常见 code</th><th>处理建议</th></tr></thead><tbody><tr><td>400</td><td><code>invalid_json</code> / <code>invalid_request</code></td><td>检查 JSON、模型字段、请求头和参数边界。</td></tr><tr><td>401</td><td><code>invalid_api_key</code></td><td>检查密钥是否完整、未撤销且属于该项目。</td></tr><tr><td>402</td><td><code>insufficient_balance</code></td><td>查看余额和充值状态；浏览器不能自行入账。</td></tr><tr><td>403</td><td><code>model_not_allowed</code> / <code>provider_region_unavailable</code> / <code>provider_commercial_unavailable</code> / <code>provider_pricing_disabled</code> / <code>subscription_entitlement_required</code></td><td>检查项目授权、地区、Provider 和套餐权益。</td></tr><tr><td>404</td><td><code>model_not_found</code></td><td>从项目模型列表复制当前别名。</td></tr><tr><td>413</td><td><code>request_too_large</code></td><td>缩小请求体或分批处理。</td></tr><tr><td>429</td><td><code>rate_limit_exceeded</code> / <code>subscription_rate_limit_exceeded</code></td><td>尊重 <code>Retry-After</code> 并采用指数退避。</td></tr><tr><td>502</td><td><code>provider_error</code></td><td>使用请求 ID 排查上游响应；避免无界重试。</td></tr><tr><td>503</td><td><code>provider_unavailable</code> / <code>service_unavailable</code></td><td>查看公开状态并做有上限的退避重试。</td></tr></tbody></table></div></section><section id="rate-limits"><h2>限流</h2><p>密钥维度使用 RPM 和 TPM；订阅还可施加组织维度请求速率与并发限制。成功或限流响应可能包含：</p><pre className="docs-code-static"><code>{`X-RateLimit-Limit-Requests\nX-RateLimit-Remaining-Requests\nX-RateLimit-Limit-Tokens\nX-RateLimit-Remaining-Tokens\nX-RelayDock-Subscription-RateLimit-Limit\nX-RelayDock-Subscription-RateLimit-Remaining\nRetry-After`}</code></pre><p>收到 429 后等待 <code>Retry-After</code>，加入抖动并设置最大重试次数。不要通过更换账户、密钥、代理或地区规避限制。</p></section><section id="idempotency"><h2>幂等</h2><p>推理 POST 可选发送最长 200 字符的 <code>Idempotency-Key</code>。对同一逻辑请求重试时复用同一 key；不同请求使用不同 key。</p><pre className="docs-code-static"><code>Idempotency-Key: 8f56a91a-9670-4eb6-8c02-10de96e86b35</code></pre><p>同 key 同载荷的后续请求返回 <code>409 idempotent_replay</code>，并通过 <code>X-RelayDock-Original-Request-Id</code> 指向首次请求。网关不会缓存或回放模型输出；同 key 不同载荷返回幂等冲突。客户端超时后应使用原请求 ID 调查结果，不要把冲突响应当成模型结果。</p></section><section id="billing"><h2>计费</h2><p>订阅费、Token 按量费与支付渠道或平台服务费是独立项目。每次可计费请求会保存不可变价格快照，并在用量、钱包交易和账本中关联 RelayDock 请求 ID。</p><ul className="docs-checklist"><li><CheckCircle2 />调用前：在公开定价与模型目录核对地区、币种、单位、税和退款摘要。</li><li><CheckCircle2 />调用后：在控制台用量页查看 Token，在账务页查看最终十进制扣费。</li><li><CheckCircle2 />公开页中的赠送金额仅是已发布条款披露，不代表自动发放或已经到账；实际可用额度只以账户 promotion_credit 和钱包账本入账为准，且已入账赠送额度可能不可现金退款。</li><li><CheckCircle2 />控制台估算或公开单价不是 Provider 发票；最终证据以请求价格快照和账本为准。</li></ul><Link className="docs-action-link" to="/pricing">查看公开价格<ArrowRight size={14} /></Link></section><section id="byok"><h2>BYOK</h2><p>BYOK 仅用于组织拥有或通过合同获授权的官方 Provider API 凭据。凭据加密保存且不会通过 API 再次返回。</p><div className="docs-callout"><ShieldCheck size={17} /><div><strong>所有权与 Provider 条款仍然适用</strong><p>BYOK 不允许密钥买卖、共享消费者账户、自动批量注册、验证码绕过、代理规避、地区限制绕过或安全机制绕过。Provider 合同、地区和 kill switch 仍会在调用前执行。</p></div></div><Link className="docs-action-link" to="/console/byok">打开 BYOK 设置<ArrowRight size={14} /></Link></section><section id="retention"><h2>数据保留</h2><p>控制台请求日志展示脱敏元数据，不展示提示或响应正文。具体日志保留天数、审计保留、跨境路由与 Provider 数据保留政策取决于组织设置和当前 Provider 披露；不要从通用文档推断固定期限。</p><ul className="docs-checklist"><li><CheckCircle2 />在 Provider 目录核对 <code>data_processing_regions</code> 与数据保留政策。</li><li><CheckCircle2 />在账户隐私设置中核对组织/用户数据分类、保留和跨境路由。</li><li><CheckCircle2 />可通过控制台请求导出或生命周期处理；处理状态以服务端记录为准。</li></ul><Link className="docs-action-link" to="/legal/data-processing">阅读数据处理说明<ArrowRight size={14} /></Link></section><section id="fallback"><h2>Provider fallback 行为</h2><p>管理员配置 fallback group 后，主凭据组的受约束选择返回错误时，网关会在首次上游尝试前改查 fallback group。主组已选出凭据但在尚未取得任何 HTTP 响应时发生非客户端取消、非超时的连接错误，网关也可能释放主选择并最多进行一次 fallback group 上游尝试；候选仍须满足 Provider 合同、地区、组织、模型、凭据所有权、预算和健康约束。</p><div className="docs-fallback-flow"><span>项目模型别名</span><ArrowRight /><span>主组选择错误或响应前连接错误</span><ArrowRight /><span>受约束 fallback group</span><ArrowRight /><span>响应或明确失败</span></div><ul className="docs-checklist"><li><CheckCircle2 />一旦取得任何上游 HTTP 响应或 SSE 已开始，网关不再切换；Provider HTTP 4xx/5xx 不触发 fallback。</li><li><CheckCircle2 />客户端取消和请求超时不触发第二次上游尝试。</li><li><CheckCircle2 />响应前连接错误具有歧义：上游可能已经处理主请求，随后的一次 fallback 可能造成上游重复执行；平台不承诺消除此风险。</li><li><CheckCircle2 />两个组最终都无法完成选择或连接时返回 Provider 错误；实际选择与资金尝试仍写入请求、账务和审计证据。</li></ul></section><footer className="developer-docs-footer"><div><KeyRound size={18} /><span><strong>准备好开始？</strong><small>完成控制台向导并使用当前项目发布的模型别名。</small></span></div><Link className="public-button primary" to="/console/onboarding">打开 onboarding<ArrowRight size={15} /></Link></footer></article></div></div>
}

function CodeTabs({ value, onChange, code, onCopy }: { value: ExampleLanguage; onChange: (value: ExampleLanguage) => void; code: string; onCopy: () => void }) {
  return <div className="docs-code-tabs"><header><div>{(['curl', 'python', 'typescript'] as const).map((item) => <button type="button" className={item === value ? 'active' : ''} onClick={() => onChange(item)} key={item}>{item === 'typescript' ? 'JavaScript / TypeScript' : item === 'curl' ? 'cURL' : 'Python'}</button>)}</div><Button size="sm" variant="ghost" onClick={onCopy}><Copy size={13} />复制</Button></header><pre><code>{code}</code></pre></div>
}
