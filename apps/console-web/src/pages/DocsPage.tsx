import { useState } from 'react'
import { ArrowUpRight, Check, Copy, KeyRound, Network, ShieldCheck, Terminal } from 'lucide-react'
import { Button, Panel, useToast } from '../components/ui'

const examples = {
  python: `from openai import OpenAI\n\nclient = OpenAI(\n    api_key="rdk_live_your_key",\n    base_url="http://localhost:8080/v1"\n)\n\nresponse = client.responses.create(\n    model="gpt-default",\n    input="Say hello from RelayDock"\n)\n\nprint(response.output_text)`,
  javascript: `import OpenAI from "openai";\n\nconst client = new OpenAI({\n  apiKey: process.env.RELAYDOCK_API_KEY,\n  baseURL: "http://localhost:8080/v1",\n});\n\nconst response = await client.responses.create({\n  model: "gpt-default",\n  input: "Say hello from RelayDock",\n});\n\nconsole.log(response.output_text);`,
  curl: `curl http://localhost:8080/v1/responses \\\n  -H "Authorization: Bearer $RELAYDOCK_API_KEY" \\\n  -H "Content-Type: application/json" \\\n  -d '{\n    "model": "gpt-default",\n    "input": "Say hello from RelayDock"\n  }'`,
}

export function DocsPage() {
  const [language, setLanguage] = useState<keyof typeof examples>('python')
  const toast = useToast()
  return <div className="docs-layout"><aside className="docs-toc"><strong>GET STARTED</strong><a href="#quickstart">Quickstart</a><a href="#authentication">Authentication</a><a href="#models">Model aliases</a><strong>API REFERENCE</strong><a href="#responses">Responses</a><a href="#chat">Chat Completions</a><a href="#embeddings">Embeddings</a><a href="#errors">Errors</a></aside><article className="docs-content"><div className="docs-hero"><span className="docs-icon"><Network size={21} /></span><div><span className="eyebrow">RELAYDOCK API</span><h1>Developer documentation</h1><p>Connect an OpenAI-compatible SDK to RelayDock, choose a stable model alias, and start sending requests.</p></div></div><section id="quickstart"><h2>Quickstart</h2><p>RelayDock exposes an OpenAI-compatible base URL. Choose your SDK, provide a RelayDock API key through an environment variable, and use an alias enabled for your workspace.</p><div className="doc-code"><header><div>{(['python', 'javascript', 'curl'] as const).map((value) => <button className={language === value ? 'active' : ''} onClick={() => setLanguage(value)} key={value}>{value === 'curl' ? 'cURL' : value[0].toUpperCase() + value.slice(1)}</button>)}</div><Button size="sm" variant="ghost" onClick={() => { void navigator.clipboard.writeText(examples[language]); toast('Example copied') }}><Copy size={13} />Copy</Button></header><pre><code>{examples[language]}</code></pre></div></section><section id="authentication"><h2>Authentication</h2><p>Send a RelayDock API key as a Bearer token. Keep it on the server and load it from a secret manager or environment variable.</p><Panel className="doc-callout"><KeyRound size={18} /><div><strong>Use downstream RelayDock keys</strong><p>Never place an upstream provider credential in application requests. RelayDock selects an authorized provider credential internally.</p></div></Panel><div className="doc-code compact"><pre><code>Authorization: Bearer rdk_live_your_key</code></pre></div></section><section id="models"><h2>Model aliases</h2><p>Aliases such as <code>gpt-default</code> keep application configuration stable while administrators manage upstream model routes.</p><ul className="doc-checklist"><li><Check size={14} />Your app sends the configured alias.</li><li><Check size={14} />RelayDock resolves the current provider model.</li><li><Check size={14} />The scheduler selects an eligible credential.</li><li><Check size={14} />Usage and request metadata are recorded.</li></ul></section><section id="responses"><h2>Responses API</h2><p>Use <code>POST /v1/responses</code> for new text and tool-enabled integrations. Streaming is supported with <code>stream: true</code>.</p></section><section id="chat"><h2>Chat Completions</h2><p>Existing OpenAI-compatible integrations can use <code>POST /v1/chat/completions</code>, including server-sent event streaming.</p></section><section id="embeddings"><h2>Embeddings</h2><p>Send embedding workloads to <code>POST /v1/embeddings</code> with an alias enabled for your workspace.</p></section><section id="errors"><h2>Errors</h2><p>Errors follow the OpenAI-compatible <code>{'{ "error": { "message", "type", "code" } }'}</code> envelope where possible. Use the RelayDock request ID when contacting an administrator.</p><div className="docs-footer-links"><a href="/playground"><Terminal size={15} />Open Playground<ArrowUpRight size={13} /></a><a href="/api-keys"><ShieldCheck size={15} />Manage API keys<ArrowUpRight size={13} /></a></div></section></article></div>
}
