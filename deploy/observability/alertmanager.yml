route:
  receiver: local-operations
  group_by: [alertname, severity]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: local-operations

# Production notification credentials belong in a separately mounted secret
# file.  Do not place webhook URLs, SMTP passwords, or paging tokens here.
